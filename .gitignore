# Dependências
node_modules/
# Build
dist/
build/
# Ambiente
.env
.env.local
*.local
# Ferramenta de diagramas (baixada pelo gerar.sh)
docs/diagramas/.plantuml.jar
# Sistema
.DS_Store
Thumbs.db

# Config local do Claude
.claude/

# Cache de build TS
*.tsbuildinfo

# Config de runtime (por ambiente) — versionar apenas o config.example.json
front/public/config.json
